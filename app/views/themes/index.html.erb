<h1>Общий список тем</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

  <% @themes.each do |theme| %>
      <tr>
        <td><%= link_to 'Показать', theme %></td>
        <td><%= link_to 'Редактировать', edit_theme_path(theme) %></td>
        <td><%= link_to 'Удалить', theme, confirm: 'Вы уверены?', method: :delete %></td>
      </tr>
  <% end %>
</table>

<br />

<%= link_to 'Создать тему', new_theme_path %>
