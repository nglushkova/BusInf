<h1>Listing book_of_classes</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @book_of_classes.each do |book_of_class| %>
  <tr>
    <td><%= link_to 'Show', book_of_class %></td>
    <td><%= link_to 'Edit', edit_book_of_class_path(book_of_class) %></td>
    <td><%= link_to 'Destroy', book_of_class, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Book of class', new_book_of_class_path %>
